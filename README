-To use this you have to install the latest requests module:
pip install requests
or:
easy_install requests

-Also _winreg is a module for Python 2.7 which has been renamed in Python 3 so be aware of that.